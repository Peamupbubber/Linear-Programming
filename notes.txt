If a token is ever added to the scanner make sure to change the grammar,
parser, scanner, token names, and reservered words list if needed.


Somewhere IDs need to be ensured that they match the given variables

If I implement non negative vars, make sure it is processed like a constraint

In linear sum, ensure the - is processed in the following product


NumberFormatException can occur for decimals too long, consider storing all constants in linked lists?

At the moment, decimal fractions dont set the values properly, scoef will try to overwrite the value